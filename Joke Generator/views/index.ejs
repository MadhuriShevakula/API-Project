<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Joke Generator</title>
    <link rel="stylesheet" type="text/css" href="main.css">
</head>
<body>
    <div class="main-container">

        <h1>Joke Generator</h1>

        <form action="/generate-joke" method="post" class="joke-generator-form">

            <label for="categories">Select joke categories:</label>
            <select name="categories" id="categories" multiple>
                <option value="Misc">Miscellaneous</option>
                <option value="Programming">Programming</option>
                <option value="Pun">Pun</option>
                <option value="Spooky">Spooky</option>
                <option value="Christmas">Christmas</option>
                <option value="Any">Any</option>
            </select>

            <label for="languages">Select joke language:</label>
            <select name="languages" id="languages">
                <option value="es">es</option>
                <option value="en" selected>en</option>
                <option value="de">de</option>
                <option value="fr">fr</option>
                <option value="pt">pt</option>
                <option value="cs">cs</option>
            </select>

            <label for="blackListFLags">Select blacklist flags:</label>
            <select name="blackListFLags" id="blackListFLags">
                <option value="" disabled selected>Select BlackListFlag</option>
                <option value="nsfw">nsfw</option>
                <option value="religious">Religious</option>
                <option value="political">Political</option>
                <option value="racist">Racist</option>
                <option value="sexist">Sexist</option>
                <option value="explicit">Explicit</option>
            </select>

            <label for="joke-type">Select joke type:</label>
            <select name="joke-type" id="joke-type">
                <option value="" disabled selected>Select Joke Type</option>
                <option value="single">Single</option>
                <option value="twopart">Two Part</option>
            </select>

            <button type="submit">Generate Joke</button>
        </form>

        <div class="joke-container">
            <h2>Generated Joke:</h2>
            <% if(locals.setup || locals.delivery){ %>
                <div class="joke-generated">
                    <% if(locals.setup) { %>
                        <p><strong>Setup:</strong> <%= setup %></p>
                    <% } %>
                    <% if(locals.delivery) { %>
                        <p><strong>Delivery:</strong> <%= delivery %></p>
                    <% } %>
                </div>
            <% } else if(locals.joke) { %>
                <div class="joke-generated">
                    <p><%= locals.joke %></p>
                </div>
            <% } %>
            <% if(locals.error){ %>
                <div class="error-generated">
                    <p><%= locals.error %></p>
                </div>
            <% } %>
        </div>
    </div>
    
</body>
</html>